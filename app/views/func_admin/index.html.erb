<h1>FuncAdmin#index</h1>
<p>Find me in app/views/func_admin/index.html.erb</p>

<% if can? :crud, FuncAdmin %>
    <% @func_admin.each do |func| %>
        <li><%= func.id %></li>
    <% end %>
<% end %>